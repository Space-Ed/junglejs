import {j, J} from '../../../jungle'


describe('navigation', function(){

    let mover, root

    beforeAll(function(){
        
    })

    beforeEach(function(){

    })
    

    it('should move to a valid subcomposite',function(){



    })

    it('should move to a valid parent')

    it('should move to root')

    it('should move to an adjacent composite')
   
    it('should fail to move up at root')

    it('should fail to move to a non existant subcomposite')

    it('should fail to move to a non composite')
})